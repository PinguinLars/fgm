# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G

#org.gradle.configuration-cache=true #this was tested on `./gradlew runClient` and it works, but there is a chance it can break on other tasks

# Fabric Properties
# check these on https://fabricmc.net/develop
# Note that this variable does *not* define the minimum required version in fabric.mod.json!
minecraft_version=1.21.7
yarn_build=1
loader_version=0.16.14

# Mod Properties
mod_version = 4.3.4
maven_group = com.wildfiregender.main
archives_base_name = Female-Gender-Mod

# Dependencies
fabric_version=0.128.1+1.21.7
